{
	"name": "rest-rpc",
	"private": true,
	"homepage": "https://github.com/rest-rpc/rest-rpc#readme",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/rest-rpc/rest-rpc.git"
	},
	"type": "module",
	"scripts": {
		"build:packages": "pnpm -r --filter './packages/**' build",
		"typecheck": "pnpm run typecheck:workspace",
		"typecheck:workspace": "pnpm run build:packages && pnpm -r --parallel --no-bail --filter './packages/**' --filter './declaration-types/**' --filter '!integration-tests' typecheck",
		"test": "pnpm run test:unit && pnpm run test:integration",
		"test:unit": "pnpm run build:packages && pnpm run test:scripts && pnpm -r --parallel --no-bail --if-present --filter './packages/**' --filter '!integration-tests' test",
		"test:integration": "pnpm run build:packages && pnpm --filter integration-tests test",
		"test:scripts": "node --test scripts/*.test.mjs",
		"lint": "oxlint",
		"lint:verify": "pnpm run lint",
		"lint:staged": "lint-staged",
		"lint:write": "pnpm run lint:staged",
		"lint:unsafe": "oxlint --fix-dangerously",
		"fmt": "oxfmt",
		"fmt:check": "oxfmt --check",
		"changeset:version": "changeset version && pnpm install --lockfile-only --no-frozen-lockfile",
		"release": "pnpm run build:packages && changeset publish",
		"release:github:notes": "node scripts/create-github-release.ts --notes",
		"bench:typecheck": "pnpm run build:packages && pnpm --filter benchmarks bench:typecheck",
		"api:docs": "pnpm run build:packages && pnpm run api:docs:verify",
		"api:docs:verify": "node scripts/check-public-api-docs.mjs",
		"readme:sync": "node scripts/sync-readmes.cjs",
		"check": "pnpm run lint && pnpm run typecheck && pnpm run api:docs:verify && pnpm run test",
		"check:verify": "pnpm run lint:verify && pnpm run typecheck && pnpm run api:docs:verify && pnpm run test",
		"docs:dev": "pnpm run build:packages && pnpm --filter content dev",
		"docs:build": "pnpm run build:packages && pnpm --filter content build",
		"docs:preview": "pnpm run build:packages && pnpm --filter content preview"
	},
	"devDependencies": {
		"@changesets/cli": "^3.0.0",
		"lefthook": "2.1.5",
		"lint-staged": "17.3.0",
		"oxfmt": "0.65.0",
		"oxlint": "1.80.0",
		"tsd": "^0.33.0",
		"typescript": "^7.0.2",
		"typescript-compiler-api": "npm:typescript@5.9.3"
	},
	"lint-staged": {
		"*": "oxfmt --no-error-on-unmatched-pattern",
		"*.{js,jsx,ts,tsx,mjs,cjs}": "oxlint --fix --no-error-on-unmatched-pattern"
	},
	"packageManager": "pnpm@10.32.1"
}
