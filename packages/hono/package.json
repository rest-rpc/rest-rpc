{
	"name": "@contract-first-api/hono",
	"version": "4.4.0",
	"type": "module",
	"license": "MIT",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"files": [
		"dist"
	],
	"scripts": {
		"build": "pnpm --filter @contract-first-api/express build && tsc -p tsconfig.build.json",
		"prepack": "pnpm run build",
		"prepublishOnly": "node ../../scripts/ensure-pnpm-publish.cjs",
		"typecheck": "pnpm run build && tsc -p tsconfig.json",
		"test": "pnpm run build && node --test src/**/*.test.ts"
	},
	"dependencies": {
		"@contract-first-api/core": "workspace:^",
		"@contract-first-api/express": "workspace:^"
	},
	"peerDependencies": {
		"hono": "^4.0.0"
	}
}
