{
	"name": "@rest-rpc/fastify",
	"version": "0.1.0-beta.14",
	"description": "Fastify adapter for serving rest-rpc contracts with typed HTTP and WebSocket routes.",
	"keywords": [
		"api-contract",
		"contract-first",
		"fastify",
		"fastify-websocket",
		"http",
		"middleware",
		"rest",
		"rest-rpc",
		"rpc",
		"server",
		"type-safe-api",
		"typescript",
		"websocket"
	],
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/rest-rpc/rest-rpc.git",
		"directory": "packages/fastify"
	},
	"files": [
		"dist"
	],
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"scripts": {
		"build": "tsc -p tsconfig.build.json",
		"typecheck": "pnpm run build && tsc -p tsconfig.json"
	},
	"dependencies": {
		"@rest-rpc/core": "workspace:*",
		"@rest-rpc/server": "workspace:*"
	},
	"devDependencies": {
		"@fastify/websocket": "^11.0.0",
		"fastify": "^5.0.0"
	},
	"peerDependencies": {
		"@fastify/websocket": "^11.0.0",
		"fastify": "^5.0.0"
	},
	"peerDependenciesMeta": {
		"@fastify/websocket": {
			"optional": true
		}
	}
}
