{
	"name": "@rest-rpc/fastify",
	"version": "0.1.0-beta.19",
	"description": "Fastify adapter for serving rest-rpc contracts with typed HTTP routes.",
	"keywords": [
		"api-contract",
		"contract-first",
		"fastify",
		"http",
		"middleware",
		"rest",
		"rest-rpc",
		"rpc",
		"server",
		"type-safe-api",
		"typescript"
	],
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/rest-rpc/rest-rpc.git",
		"directory": "packages/fastify"
	},
	"files": [
		"dist"
	],
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"scripts": {
		"build": "tsc -p tsconfig.build.json",
		"typecheck": "pnpm run build && tsc -p tsconfig.json"
	},
	"dependencies": {
		"@rest-rpc/core": "workspace:*",
		"@rest-rpc/node": "workspace:*",
		"@rest-rpc/server": "workspace:*"
	},
	"devDependencies": {
		"fastify": "^5.0.0"
	},
	"peerDependencies": {
		"fastify": "^5.0.0"
	}
}
