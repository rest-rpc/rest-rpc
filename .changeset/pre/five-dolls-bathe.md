---
"@rest-rpc/core": minor
"@rest-rpc/tanstack-query": minor
---

Replace `.fetch` and `.fetchResponse` with callable route leaves that return the response envelope. Server-first HTTP selectors now accept the path, grouped request, and Fetch API options in one call.
